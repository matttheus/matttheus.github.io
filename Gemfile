source "https://rubygems.org"

# A gem github-pages trava o Jekyll, o tema minima e os plugins nas mesmas
# versões que o GitHub Pages usa em produção, evitando surpresas no deploy.
gem "github-pages", group: :jekyll_plugins

# Necessário para o `jekyll serve` em Ruby 3+.
gem "webrick"
